package ejemplo;

public interface Estadio {
	
	//tenemos que utilizar un método por lo menos que no tenga código (que no esté implementado)
	
	//Si tenemos una interfaz sabemos que tenemos métodos sin implementar, pero los podemos utilizar con implements
	
	//La clase abstracta no puede generar objetos, la clase normal o concreta, sí. Además puede implementar una ionterface y heredar de esta
	
	
	public void abrirEstadio();
	public void cerrarEstadio();
	void metodo();

}
